apiVersion: v2
description: A Helm chart for Codefresh Runner
name: cf-runtime
version: 10.3.7
keywords:
  - codefresh
  - runner
home: https://codefresh.io/
icon: https://avatars1.githubusercontent.com/u/11412079?v=3
sources:
  - https://github.com/codefresh-io/venona
maintainers:
  - name: codefresh
    url: https://codefresh-io.github.io/
annotations:
  # 💡 Do not forget to update this annotation:
  artifacthub.io/containsSecurityUpdates: "true"
  # Supported kinds: `added`, `changed`, `deprecated`, `removed`, `fixed`, `security`:
  artifacthub.io/changes: |
    - kind: security
      description: 'Set "engine" to "3.2.25" tag. Security fixes.'
    - kind: security
      description: 'Set "cli" to "1.2.2" tag. Security fixes.'
    - kind: security
      description: 'Set "gcloud-builder" to "0.5.13" tag. Security fixes.'
    - kind: security
      description: 'Set "dind" to "3.0.19" tag. Security fixes.'
    - kind: security
      description: 'Set "cf-docker-pusher" to "6.0.32" tag. Security fixes.'
    - kind: security
      description: 'Set "cf-docker-builder" to "1.6.7" tag. Security fixes.'
    - kind: security
      description: 'Set "cf-container-logger" to "2.0.29" tag. Security fixes.'
    - kind: security
      description: 'Set "cf-k8s-agent" to "1.3.38" tag. Security fixes.'
    - kind: security
      description: 'Set "compose" to "5.2.0-1.6.7" tag. Security fixes.'
    - kind: security
      description: 'Set "pikolo" to "0.15.4" tag. Security fixes.'
    - kind: security
      description: 'Set "cf-cosign-image-signer" to "3.1.1-cf.1" tag. Security fixes.'
    - kind: security
      description: 'Set "cf-docker-puller" to "8.0.39" tag. Security fixes.'
    - kind: security
      description: 'Set "cf-app-proxy" to "0.1.2" tag. Security fixes.'
dependencies:
  - name: cf-common
    repository: oci://quay.io/codefresh/charts
    version: 0.21.0
